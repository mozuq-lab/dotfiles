alias{
    ls='ls -CF --show-control-chars --color=auto',
    ll='ls -AlFh --show-control-chars --color=auto',
    la='ls -CFal --show-control-chars --color=auto',
    mv='mv -i',
    rm='rm -i',
    cp='cp -i',
    vi='vim',
}
